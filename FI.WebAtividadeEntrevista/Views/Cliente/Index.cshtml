
@{
    ViewBag.Title = "Clientes";
}
@section scripts{
    @Styles.Render("~/Content/jtable")
    <script>
    var urlAlteracao = '@Url.Action("Alterar", "Cliente", new { area = "" })';
    var urlClienteList = '@Url.Action("ClienteList", "Cliente", new { area = "" })';

    </script>
    @Scripts.Render("~/bundles/listClientes")
}
<div class="container-fluid">
    <h2>@Html.Raw(ViewBag.Title)</h2>
    <div class="row">
        <div class="col-12 col-lg-12 d-flex justify-content-end">
            @Html.ActionLink("Novo Cliente", "Incluir", "Cliente", new { }, new { @class = "btn btn-md btn-primary" })
        </div>
    </div>
    <div class="row mt-1">
        <div class="col-12 col-lg-12">
            <table id="gridClientes" class="table table-responsive"></table>
        </div>
    </div>
</div>